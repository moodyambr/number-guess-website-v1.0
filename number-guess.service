[Unit]
Description=Number Guess Game - Spring Boot API
After=network.target docker.service
Requires=docker.service

[Service]
Type=forking
User=ec2-user
WorkingDirectory=/home/ec2-user/app

# Ladda env-variabler från .env
EnvironmentFile=/home/ec2-user/app/.env

# Sätt prod-profil
Environment=SPRING_PROFILES_ACTIVE=prod

# Starta script
ExecStart=/bin/bash /home/ec2-user/app/ec2-start.sh

# PID-fil för att systemd ska kunna stoppa processen
PIDFile=/home/ec2-user/app/app.pid

# Stoppa appen
ExecStop=/bin/bash -c 'kill $(cat /home/ec2-user/app/app.pid)'

Restart=on-failure
RestartSec=15s

StandardOutput=append:/home/ec2-user/app/app.log
StandardError=append:/home/ec2-user/app/app.log

[Install]
WantedBy=multi-user.target

